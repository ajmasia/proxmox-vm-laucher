name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target universal-apple-darwin'
          - platform: 'ubuntu-22.04'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'npm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm ci

      - name: Build the app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: '🚀 pvel ${{ github.ref_name }}'
          releaseBody: |
            ## 📦 What's New

            Easy way to connect and manage your Proxmox virtual machines using SPICE protocol.

            ---

            ## 💾 Installation

            ### 🐧 Linux
            Download the `.deb` package for Debian/Ubuntu or the `.AppImage` for other distributions.

            **Debian/Ubuntu:**
            ```bash
            sudo dpkg -i pvel_${{ github.ref_name }}_amd64.deb
            ```

            **AppImage:**
            ```bash
            chmod +x pvel_${{ github.ref_name }}_amd64.AppImage
            ./pvel_${{ github.ref_name }}_amd64.AppImage
            ```

            ### 🍎 macOS
            Download the `.dmg` file and drag **pvel** to your Applications folder.

            ---

            ## ✨ Features

            - 🖥️ Connect to Proxmox VMs via SPICE protocol
            - 🔒 Secure credential storage
            - 🎯 Filter VMs by status, tags, and SPICE availability
            - ⚡ Fast VM operations (start, stop, suspend)
            - 🎨 Clean and intuitive UI

            ---

            ## 📝 Full Changelog

            See the commits included in this release for detailed changes.
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
